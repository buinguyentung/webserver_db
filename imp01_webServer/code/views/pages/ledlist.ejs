<!DOCTYPE html>
<html lang="en">
<head>
	<% include ../partials/head %>
</head>
<body class="container">

	<header>
		<% include ../partials/header %>
	</header>

	<main>
        <h1>LED</h1>
        <table class="table table-striped">
            <thead>
              <tr>
                <td><b>ID</b></td>
                <td colspan="2"><b>State</b></td>
              </tr>
            </thead>
            <tbody>
              <% for (var i=0; i < itms.length; i++) { %>
              <% var led_id = itms[i].led_id; %>
              <% if (led_id.indexOf("LED") + 1) { %>
              <tr>
                <td><%= itms[i].led_id %></td>
                <td><%= itms[i].led_state %></td>
                <td>
                  <% var led_st = itms[i].led_state; %>                  
                  <% if (led_st == "ON") { %>
                    <a href="/led/ledswitch/<%= itms[i]._id %>" class="btn btn-danger">Off</a>
                  <% } %>
                  <% if (led_st == "OFF") { %>
                    <a href="/led/ledswitch/<%= itms[i]._id %>" class="btn btn-primary">On</a>
                  <% } %>
                </td>
              </tr>
              <% } %>
              <% } %>
            </tbody>
          </table>
        <hr/>

        <h2>TEMPERATURE</h2>
        <table class="table table-striped">
            <thead>
              <tr>
                <td><b>ID</b></td>
                <td colspan="2"><b>Value</b></td>
              </tr>
            </thead>
            <tbody>
              <% for (var i=0; i < itms.length; i++) { %>
              <% var led_id = itms[i].led_id; %>
              <% if (led_id.indexOf("TEMP") + 1) { %>
              <tr>
                <td><%= itms[i].led_id %></td>
                <td><%= itms[i].led_state %></td>
              </tr>
              <% } %>
              <% } %>
            </tbody>
          </table>
        <hr/>

        <h2>HUMIDITY</h2>
        <table class="table table-striped">
            <thead>
              <tr>
                <td><b>ID</b></td>
                <td colspan="2"><b>Value</b></td>
              </tr>
            </thead>
            <tbody>
              <% for (var i=0; i < itms.length; i++) { %>
              <% var led_id = itms[i].led_id; %>
              <% if (led_id.indexOf("HUMID") + 1) { %>
              <tr>
                <td><%= itms[i].led_id %></td>
                <td><%= itms[i].led_state %></td>
              </tr>
              <% } %>
              <% } %>
            </tbody>
          </table>
        <hr/>
	</main>

	<footer>
		<% include ../partials/footer %>
	</footer>
	
</body>
</html>